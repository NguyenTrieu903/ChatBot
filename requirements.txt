# Vietnamese Medical Chatbot with ChromaDB + LangChain + Groq AI
# Free & Fast local vector database with cloud LLM

# Groq AI (fast & free)
groq>=0.11.0

# LangChain
langchain>=0.1.0
langchain-groq>=0.2.0
langchain-community>=0.0.10
langchain-text-splitters>=0.2.0  # Text chunking
langchain-chroma>=0.1.0  # ChromaDB integration
langchain-huggingface>=0.1.0  # HuggingFace embeddings (updated)

# Vector database - ChromaDB (Free, Local, No API key needed!)
chromadb>=0.4.22

# Embeddings - HuggingFace multilingual model
sentence-transformers>=2.2.0  # For multilingual-e5-large embeddings

# Web UI
streamlit>=1.28.0

# Basic utilities
python-dotenv>=1.0.0
numpy>=1.24.0

# Document processing
openpyxl>=3.1.0  # Excel file only (Word file not used)