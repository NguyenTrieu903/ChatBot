# Vietnamese Chatbot with Groq AI (Free & Fast!)
# Uses PineconeEmbeddings for multilingual support

# Groq AI (fast & free)
groq>=0.11.0

# LangChain
langchain>=0.1.0
langchain-groq>=0.2.0
langchain-community>=0.0.10
langchain-pinecone>=0.2.0  # Pinecone integration with PineconeEmbeddings
langchain-text-splitters>=0.2.0  # Text chunking

# Vector database - Pinecone (Production-ready, scalable)
# Pin to version 5.x to avoid deprecated plugin errors
# Note: If you see DeprecatedPluginError, uninstall any old pinecone plugins:
# pip uninstall pinecone-plugin-inference -y
pinecone>=5.0.0,<6.0.0

# Web UI
streamlit>=1.28.0

# Basic utilities
python-dotenv>=1.0.0
numpy>=1.24.0

# Document processing
openpyxl>=3.1.0  # Excel file only (Word file not used)