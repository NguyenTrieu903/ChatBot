# Vietnamese Chatbot with Groq AI (Free & Fast!)
# Local embeddings - No API limits!

# Groq AI (fast & free)
groq>=0.11.0

# LangChain
langchain>=0.1.0
langchain-groq>=0.2.0
langchain-community>=0.0.10
langchain-pinecone>=0.2.0  # New Pinecone integration (better compatibility)
langchain-text-splitters>=0.2.0  # Text chunking

# Local Embeddings (No API needed!)
sentence-transformers>=2.2.0
torch>=2.0.0
langchain-huggingface>=0.0.1  # Updated embeddings (fixes deprecation warning)

# Vector database - Pinecone (Production-ready, scalable)
pinecone
# faiss-cpu>=1.8.0  # Optional: Keep for local development/testing

# Web UI
streamlit>=1.28.0

# Basic utilities
python-dotenv>=1.0.0
numpy>=1.24.0

# Document processing
openpyxl>=3.1.0  # Excel file only (Word file not used)